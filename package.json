{
  "name": "the-falcon-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev",
    "lint": "dotenv -- turbo lint",
    "test": "dotenv -- turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "globals": "^17.3.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.3"
  },
  "packageManager": "pnpm@9.0.0"
}
